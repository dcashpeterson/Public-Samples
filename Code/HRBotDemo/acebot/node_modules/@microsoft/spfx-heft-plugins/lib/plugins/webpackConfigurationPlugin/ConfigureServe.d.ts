/// <reference types="express-serve-static-core" />
import type { Request, Response, NextFunction } from 'express';
import { type ITerminal } from '@rushstack/node-core-library';
import type { HeftConfiguration } from '@rushstack/heft';
export interface IApiMap {
    [route: string]: (req: Express.Request, resp: Express.Response) => void;
}
export interface IServeConfig {
    port: number;
    contentBase?: string | string[];
    contentBasePublicPath?: string;
    initialPage?: string;
    apiMap?: IApiMap;
}
export declare class ConfigureServe {
    private _buildFolder;
    private _terminal;
    private _serveConfig;
    constructor(configuration: HeftConfiguration, terminal: ITerminal);
    generateServeConfig(): IServeConfig;
    logRequestsMiddleware(): (req: Request, res: Response, next: NextFunction) => void;
    private _getServeConfig;
}
//# sourceMappingURL=ConfigureServe.d.ts.map