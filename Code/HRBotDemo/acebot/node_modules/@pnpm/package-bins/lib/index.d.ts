import { DependencyManifest } from '@pnpm/types';
export interface Command {
    name: string;
    path: string;
}
export default function binify(manifest: DependencyManifest, pkgPath: string): Promise<Command[]>;
